project LibSodium is
    for Languages use ("Ada","C");
    for Source_Dirs use ("common","libsodium_body/**","include/**");
    for Object_Dir use "obj";

   for Main use ("test.adb");

   package Compiler is
      for Switches ("ada") use ("-gnata", "-gnatwa", "-gnaty3M79SabCdefhiklnprstux");
   end Compiler;

end Libsodium;
